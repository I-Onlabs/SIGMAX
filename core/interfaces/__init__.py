"""
Core interface layer (channel adapters + structured contracts).

This package provides stable, UI/channel-agnostic entrypoints into SIGMAX:
- Typed request/response contracts
- ChannelService: routes structured requests into the orchestrator and returns artifacts
"""

